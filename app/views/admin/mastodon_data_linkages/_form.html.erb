<%= form_with(model: mastodon_data_linkage, url: admin_mastodon_data_linkage_path ) do |form| %>
  <% if mastodon_data_linkage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mastodon_data_linkage.errors.count, "error") %> prohibited this creator from being saved:</h2>

      <ul>
        <% mastodon_data_linkage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :site, class: 'form-label' %>
    <%= form.text_field :site, class: 'form-control' %>
  </div>

  <div>
    <%= form.label :token, class: 'form-label' %>
    <%= form.text_field :token, class: 'form-control' %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
