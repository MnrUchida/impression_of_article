<%= form_with(url: admin_shared_musics_path, model: music, data: { action: "turbo:submit-end->modal#close" }, method: :post) do |form| %>
  <% if music.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(music.errors.count, "error") %> prohibited this music from being saved:</h2>

      <ul>
        <% music.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, 'タイトル', class: 'form-label' %>
    <%= form.text_field :title, class: 'form-control' %>
    <%= form.hidden_field :url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
