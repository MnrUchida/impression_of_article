<div class="container" >
  <div class="js-impression">
    <%= link_to "戻る", my_page_impression_path(@impression) %>
    <%= link_to "タグ追加", new_my_page_impression_tag_path(impression_id: @impression.id), data: { turbo_frame: "modal" } %>
  </div>

  <%= render partial: 'impression', locals: { impression: @impression } %>
  <div class="js-impression">
    <%= render partial: 'impression_tags', locals: { container_id: 'impression_tags', impression_tags: @impression_tags, impression: @impression } %>
  </div>
  <%= render partial: 'tags', locals: { container_id: 'all_tags', tags: @tags, impression: @impression } %>
</div>
<%= turbo_frame_tag "modal" %>
