<div class="container js-impression" >
  <p style="color: green"><%= notice %></p>

  <%= link_to "一覧に戻る", my_page_tag_groups_path, class: 'btn btn-outline-secondary btn-sm mb-3' %>
  <%= render @tag_group %>

  <div class="text-center mt-1">
    <%= link_to "タグ編集", edit_tags_my_page_tag_group_path(@tag_group), class: 'btn btn-sm btn-outline-primary' %>
    <%= link_to "編集", edit_my_page_tag_group_path(@tag_group), class: 'btn btn-sm btn-outline-primary' %>
  </div>
</div>
